node_modules
dist
.output
.vinxi
pnpm-lock.yaml
package-lock.json
bun.lock
routeTree.gen.ts

# Verbatim CMS responses written by scripts/snapshot-cms.mjs. Prettier expands the
# minified payload, so `npm run format` and the next snapshot fight over the file.
src/lib/cms-snapshot/*.json
